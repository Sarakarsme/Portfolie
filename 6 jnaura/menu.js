function Menu() {
    background(50);

    drawSprite(button_start_game);

    fill(255);
    noStroke();
    textSize(72);
    textAlign(CENTER, CENTER);
    text("start game", width / 2, height / 2);

}